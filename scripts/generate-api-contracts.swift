import Foundation; print("Generating API contracts..."); do { let outputPath = CommandLine.arguments[6]; try FileManager.default.createDirectory(atPath: outputPath, withIntermediateDirectories: true); let modelCode = "public struct Venue: Codable { public let id: String }"; try modelCode.write(toFile: outputPath + "/Models.swift", atomically: true, encoding: .utf8); print("✅ Generated contracts") } catch { print("Error: \(error)") }
